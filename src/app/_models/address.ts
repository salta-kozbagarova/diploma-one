import { Country } from "./index";

export class Address {
    countries: Country[];

    getRegion(country: number|Country){
        // if(typeof country === Number.name){
        //     country = this.countries;
        // }
    }
}
