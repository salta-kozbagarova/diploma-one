// import { User } from "../../../_models";
// import { ProductImage } from "../../_models";
import { Product } from "../_interfaces";

export interface Transport extends Product {
    // id: number;
    // name: string;
    // description: string;
    // images: ProductImage[];
    // createdBy: User;
    // updatedBy: User;
    // createdAt: string;
    // updatedAt: string;
    // isActive: boolean;
    // isDeleted: boolean;
}